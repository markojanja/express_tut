<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("partials/head") %>
  </head>
  <body>
    <div class="wrapper">
      <%- include('partials/navbar') %>

      <div class="app">
        <div class="heading">
          <h2>#staypositive, leave nice coding message</h2>
        </div>

        <a class="btn btn-info" href="/new">Add new user</a>
        <ul>
          <% users.forEach(user => { %>
          <li>
            <p><%= user.note %></p>
            <span>posted by: <%- user.author %> </span>
            <span>date <%- new Date(user.createdAt).toLocaleDateString('en-GB') %> </span>
          </li>
          <% }) %>
        </ul>
      </div>
      <%- include("partials/footer") %>
    </div>
  </body>
</html>
